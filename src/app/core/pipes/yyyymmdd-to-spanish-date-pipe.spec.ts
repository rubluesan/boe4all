import { YyyymmddToSpanishDatePipe } from './yyyymmdd-to-spanish-date-pipe';

describe('YyyymmddToSpanishDatePipe', () => {
  it('create an instance', () => {
    const pipe = new YyyymmddToSpanishDatePipe();
    expect(pipe).toBeTruthy();
  });
});
